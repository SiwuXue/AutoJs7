<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/bg_bottom_sheet_rounded">

    <!-- Drag handle -->
    <View
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:background="@drawable/bg_drag_handle"/>

    <!-- Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="8dp">

        <!-- Script name / Title -->
        <TextView
            android:id="@+id/tv_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/text_log"
            android:textColor="?android:attr/textColorPrimary"
            android:textSize="16sp"
            android:textStyle="bold"
            android:ellipsize="end"
            android:maxLines="1"/>

        <!-- Action buttons -->
        <ImageButton
            android:id="@+id/btn_open_full"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?selectableItemBackgroundBorderless"
            android:src="@drawable/ic_logcat"
            android:tint="?attr/colorPrimary"
            android:contentDescription="@string/text_log"
            android:scaleType="center"/>

        <ImageButton
            android:id="@+id/btn_clear"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?selectableItemBackgroundBorderless"
            android:src="@drawable/ic_clear"
            android:tint="#666666"
            android:contentDescription="@string/text_clear"
            android:scaleType="center"/>

    </LinearLayout>

    <!-- Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="?android:attr/listDivider"/>

    <!-- Console View -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="400dp"
        android:paddingStart="4dp">

        <!--
            @Fix by fork author on Sep 16, 2026.
            ! These attributes referenced `@color/console_debug` and `@color/console_verbose`,
            ! which do not exist: the palette is named `console_view_*` in colors.xml, and the
            ! legacy names survive only as `legacy_console_*` behind the com.stardust.R
            ! redirect. Resource linking therefore failed and the branch could not be built.
            ! The values used now are not arbitrary: activity_main_inrt.xml sets the same two
            ! attributes on the same view with the literals #111214 and #BDBDBD, which are
            ! exactly console_view_debug and console_view_verbose.
            ! zh-CN: 这两个属性原本引用 `@color/console_debug` 与 `@color/console_verbose`,
            ! 而它们并不存在: 调色板在 colors.xml 中名为 `console_view_*`, 旧名仅以
            ! `legacy_console_*` 的形式存在于 com.stardust.R 重定向之后.
            ! 因此资源链接失败, 该分支根本无法构建.
            ! 现在使用的值并非随意挑选: activity_main_inrt.xml 在同一个视图上以字面量
            ! #111214 与 #BDBDBD 设置同样的两个属性, 而它们正是
            ! console_view_debug 与 console_view_verbose.
        -->
        <org.autojs.autojs.core.console.ConsoleView
            android:id="@+id/console"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:color_debug="@color/console_view_debug"
            app:color_verbose="@color/console_view_verbose"/>

    </FrameLayout>

</LinearLayout>
